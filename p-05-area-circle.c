// Given the radius r of a circle, calculate and print its area.
#include <stdio.h>
int main()
{
    int r;
    scanf("%d", &r);

    printf("Area of circle is: %f\n", 3.1416 * r * r);
    return 0;
}